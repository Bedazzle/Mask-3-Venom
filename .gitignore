# Build artifacts — regenerated by compile_mask_3.bat / sjasmplus.
# (Source kept in recompile/: py_memmaps.py, venom.labels)
recompile/*.bin
recompile/*.sna
recompile/*.tap
recompile/*.sym

# sjasmplus symbol / listing output anywhere
*.sym
*.lst
*.exp

# Python
__pycache__/
*.pyc
*.pyo

# Editor / OS cruft
*.bak
*~
*.swp
Thumbs.db
desktop.ini
.DS_Store

# Local Claude Code settings (keep shared .claude/settings.json if you commit it)
.claude/settings.local.json
